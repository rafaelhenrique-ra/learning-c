#include <stdio.h>
#include <math.h>

int main()
{
    float a, b, c;
    float delta, x1, x2;
    
    printf("Digite o valor de A: ");
    scanf("%f", &a);
    printf("Digite o valor de b: ")
    scanf("%f", &b);
    printf("Digite o valor de C: ");
    scanf("%f", &c);
    printf("A equação a ser resolvida é %f x*x + %f x")
}