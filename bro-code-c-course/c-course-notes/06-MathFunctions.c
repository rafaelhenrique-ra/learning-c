#include <stdio.h>
#include <math.h>

int main (void) {

    int A = sqrt(16);
    int B = pow(3, 3);
    int C = round(3.1415);
    int D = ceil(3.11);
    int E = floor(3.99);
    int F = fabs(-100);
    int G = log(3);
    int H = sin(45);
    int I = cos(45);
    int J = tan(45);

    printf("\n%lf", J);

    return 0;

}