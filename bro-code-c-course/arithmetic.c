#include <stdio.h>

int main(){
    /*
    arithmetic operators

    + (addition)
    - (subtraction)
    * multiplication
    / (division)
    % (modulus)
    ++ (increment)
    -- (decrement)
    == : Equal to
    != : Not equal to
    > : Greater than
    < : Less than
    >= : Greater than or equal to
    <= : Less than or equal to
    */

    int x = 5;
    int y = 2;

    float z = x / (float) y;

    printf("%.2f", z);

    /*
    Augmented Assigned Operators

    Basically, its the same as python. You just put a x+=1 and that's it.
    */

    return 0;



}